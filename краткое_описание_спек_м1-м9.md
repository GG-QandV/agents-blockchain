Три спеки в одном документе, каждая построена вокруг RISK-секций формата «инвариант → механизм принуждения → доказывающий тест»:

| Модуль     | RISK-секций | Ключевые (🔴)                                                                                                                                                                                                     |
| ---------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M4 vault   | 5           | Утечка plaintext-ключа (принуждение типами: !Clone/!Debug/Zeroizing + heap-скан), обход биометрии (auth-required на уровне железа, не кода), доменная сепарация подписей (теги в digest)                          |
| M7a crypto | 6           | **Unknown ≠ Failed** — execute типом не может вернуть Failed; nonce-дисциплина (застрявшая tx блокирует, а не обходится); неподменяемость контракта/получателя (хардкод + simulate-self-check + mutation testing) |
| M8 gate    | 6           | Replay (монотонный nonce, переживающий рестарт через лог), подпись↔agent_id, hardened-парсер mu-wire (общий с Composer-endpoint, fuzz 4 ч как CI-гейт)                                                            |

Плюс сквозная таблица трассируемости (11 рисков → тесты → класс потерь) и правило приёмки: PR в зоне RISK не мержится без зелёного соответствующего теста.

✓ Спеки M4/M7a/M8 написаны с фокусом на рисковые места и переданы файлом

Спеки трёх модулей «тихих отказов», тот же формат RISK: инвариант → принуждение → доказывающий тест:

| Модуль     | RISK-секций | Ключевые (🔴)                                                                                                                                                                                                                                                                                                                              |
| ---------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| M6 runtime | 6           | «WAL до денег» принуждается **типом** WalWritten (без WAL execute не компилируется); commit⇐финальность / rollback⇐доказанный неуход (в enum RollbackCause варианта «таймаут» нет типом); shutdown ≡ crash (крэш-эквивалентность как тест)                                                                                                 |
| M5 log     | 6           | Durable Pending (CrashMonkey-цикл 10³ с эмуляцией отключения питания); нефальсифицируемость: hash-chain + подпись + **anti-rollback** через μ.log_head (откат лога со снапшотом ловится); reconcile-таблица 18/18 комбинаций, default = Keep, «else→Failed» запрещён                                                                       |
| M9 human   | 6           | «Видимое = исполняемое»: PayConfirm из того же объекта, что уйдёт в M7a, связка intent_hash сверяется перед execute; **Approved непроизводим кодом** — auth_proof это подпись auth-required owner-ключом над hash, компрометация M9 не порождает валидное решение; purpose агента в диалоге не показывается вообще (вектор соц. инженерии) |

Добавлены: нормативная машина состояний intent'а, boot-протокол, формат лога с NonceSnapshot/TailTruncated/Alert, анти-rubber-stamping (бейдж «первый платёж»), внешний верификатор экспорта журнала. Сквозная таблица — 11 рисков, ночной chaos-CI как релизный гейт.

✓ Спеки M6/M5/M9 с фокусом на рисках написаны; полный комплект спек критичных модулей (6 из 9 + Composer) готов

Закрывающий документ комплекта — 5 частей, включая сквозной слой X (mu-common), который в модульной карте раньше значился как «прочее», но несёт собственные 🔴-риски:

| Часть       | RISK | Ключевое                                                                                                                                                                                                                                                |
| ----------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M1 core     | 4    | Ω-неизменяемость принуждается отсутствием &mut-доступа + побайтной сверкой в apply_delta; **rollback-атака** (подмена μ на старую копию с большими лимитами) ловится тройной связкой μ.log_head↔HEAD↔цепь; авто-восстановления из mu.prev нет намеренно |
| M2 omega    | 3    | Amount без impl Add — переполнение невозможно синтаксически; no_std как тест чистоты; ConnectorId — закрытый enum, «мёртвый» коннектор в Ω не создаёт Allow-пути                                                                                        |
| M3 delta    | 4    | Reservation — линейный тип (!Clone, commit/rollback по move, дважды не компилируется) + двойная бухгалтерия: объект для типов, лог для истины; threshold сравнивается с amount+gas, не с amount                                                         |
| M7b/c stubs | 3    | Тройной барьер против «просачивания» симуляции в учёт; demo_mode тотален (заголовок UI + Alert), стаб вне demo → Denied даже при разрешении в Ω                                                                                                         |
| X common    | 4    | Запрет float во всём денежном пути (линт-гейт); **два несмешиваемых времени** — у Intent поля ts нет by design; запрет wildcard-match на критичных enum                                                                                                 |

Итог комплекта из 4 документов: **34 RISK-секции (19 🔴)** — сводная карта является чек-листом security-аудита: проверка идёт по трассируемости инвариант→механизм→тест, а не «по коду вообще».

✓ Все спеки написаны: M1–M9, стабы, сквозной слой, Composer — комплект документации MVP полный
